{
    "name": "meeting-notification-service",
    "version": "1.0.0",
    "description": "Lambda function for sending meeting link notifications via SNS",
    "main": "index.js",
    "scripts": {
        "build": "tsc",
        "deploy": "npm run build && zip -r deployment.zip dist/ node_modules/",
        "test": "jest"
    },
    "dependencies": {
        "@aws-sdk/client-sns": "^3.445.0",
        "aws-lambda": "^1.0.7"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.126",
        "@types/node": "^20.8.7",
        "typescript": "^5.2.2"
    }
}
